function Header() {
  return (
    <header className="topbar panel panel-soft">
      <div>
        <p className="eyebrow">Frontend del proyecto integrador</p>
        <h1>EduPortal Frontend</h1>
      </div>

      <div className="topbar-actions">
        <span className="status-dot" />
        <span className="topbar-label">Base visual lista para migración</span>
      </div>
    </header>
  );
}

export default Header;
